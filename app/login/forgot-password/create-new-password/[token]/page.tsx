import type {Metadata} from "next";

export const metadata: Metadata = {
  title: 'Create new password',
};

export default function Page() {
  return (
    <p>Create new password page goes here</p>
  );
}
